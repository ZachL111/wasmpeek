# Review Journal

I treated `wasmpeek` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its developer tools focus without claiming live deployment or external usage.

## Cases

- `baseline`: `change width`, score 140, lane `ship`
- `stress`: `diagnostic quality`, score 266, lane `ship`
- `edge`: `review cost`, score 203, lane `ship`
- `recovery`: `safe rewrite`, score 175, lane `ship`
- `stale`: `change width`, score 141, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
