# Wasmpeek Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | change width | 140 | ship |
| stress | diagnostic quality | 266 | ship |
| edge | review cost | 203 | ship |
| recovery | safe rewrite | 175 | ship |
| stale | change width | 141 | ship |

Start with `stress` and `baseline`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `diagnostic quality` against `change width`, not the raw score alone.
